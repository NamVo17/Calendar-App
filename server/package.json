{
  "name": "lovytech-server",
  "version": "1.0.0",
  "description": "Simple JSON server for LovyTech application",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database.json --port 3001 --host 0.0.0.0",
    "dev": "json-server --watch database.json --port 3001 --host 0.0.0.0",
    "serve": "json-server --watch database.json --port 3001 --host 0.0.0.0"
  },
  "keywords": ["json-server", "api", "database"],
  "author": "LovyTech",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  }
} 